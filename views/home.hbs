<!DOCTYPE html>
<html lang="en">
{{> baseHead }}
<body>
    {{> sideHeader }}
    <div>
        <h2>สวัสดีUser</h2>
        <p>วันนี้มีไรจะถามมั้ย</p>
         
        {{#each allPost }}
        <div style="background-color: rgb(255, 255, 0);">
           <h4><a href="/p/{{this.id}}">{{ this.title }}</a></h4>
           <p><span>{{this.from}} </span><span>{{this.created}}</span></p>
          {{#if this.commentsCount }}
          <p>จำนวนคอมเมนต์:{{this.commentsCount}}</p>
          {{else}}
          <p>ยังไม่มีคอมเมนต์!!</p>
          {{/if}}
        </div>
        {{/each}}
    </div>
   
</body>
{{> sideFooter }}
</html>